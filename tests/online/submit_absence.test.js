const { test, expect } = require('@jest/globals')
const absences = require('../../absences.js')
const main = require('../../main.js')

test("date parse only removes date formatted objects", () => {

})